replicaCount: 2

image:
  repository: demo-fastapi
  tag: v1
  pullPolicy: Never

container:
  port: 8000

service:
  type: ClusterIP
  port: 80
  targetPort: 8000

resources:
  requests:
    cpu: "250m"
    memory: "256Mi"

  limits:
    cpu: "500m"
    memory: "512Mi"